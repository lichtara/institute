* text=auto eol=lf

# Treat notebooks as text for diffs
*.ipynb text diff=ipynb

# Use Git LFS for large binary assets
papers/brh-methodology/figures/* filter=lfs diff=lfs merge=lfs -text
outreach/openai-special-access/*.pdf filter=lfs diff=lfs merge=lfs -text
