"""Utilidades de visualização (rascunho)."""

import matplotlib.pyplot as plt


def placeholder_plot():
    """Exibe gráfico vazio até implementarmos visualizações reais."""
    fig, ax = plt.subplots()
    ax.set_title("Instalar dependências e substituir por visualizações significativas")
    return fig, ax
